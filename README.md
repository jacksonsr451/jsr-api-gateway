# API Gateway (FastAPI)

Starter structure for a FastAPI service managed with Poetry.

## Quick start

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

## Tests

```bash
poetry run pytest
```
